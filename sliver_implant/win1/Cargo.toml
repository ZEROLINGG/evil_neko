[package]
name = "win1"
version = "0.1.0"
edition = "2024"

[dependencies]
lib = { path = "../../lib", default-features = true, features = ["macros", "sandbox"]}


[profile.release]
#strip = "symbols"
debug = 0
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
